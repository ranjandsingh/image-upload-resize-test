# image-upload-resize-test
this is a demo nodejs app for resizing and adding watermark on every image uploaded.

## Install 
`npm install`

## Start 
`npm run start `
visit localhost:3000 to test the app
